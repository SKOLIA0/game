<%= form_with(model: team_participation) do |form| %>
  <% if team_participation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(team_participation.errors.count, "error") %> prohibited this team_participation from being saved:</h2>

      <ul>
        <% team_participation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
